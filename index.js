/**
 * @format
 */

import {AppRegistry} from 'react-native';
import InvoiceList from './src/Screens/InvoiceList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => InvoiceList);
